/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'ik.imagekit.io']
    },
    // devIndicators: false,
};

export default nextConfig;
